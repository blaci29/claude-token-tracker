/*
 * Global declarations for the Chrome extension.
 *
 * Declares the `chrome` namespace so that TypeScript doesn't complain about
 * missing definitions. In production, consider installing `@types/chrome` for
 * more complete typing.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
declare var chrome: any;