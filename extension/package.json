{
  "name": "claude-token-tracker",
  "version": "0.1.0",
  "description": "Real-time token tracking for Claude.ai",
  "scripts": {
    "build": "./build.sh",
    "compile": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/chrome": "^0.0.258"
  }
}
